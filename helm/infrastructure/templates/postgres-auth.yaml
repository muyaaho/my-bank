{{- if .Values.postgres.enabled }}
{{- if .Values.postgres.auth.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgres.auth.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.postgres.auth.name }}
    tier: infrastructure
    {{- include "infrastructure.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.postgres.auth.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: {{ .Values.postgres.auth.name }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.postgres.auth.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.postgres.auth.name }}
    tier: infrastructure
    {{- include "infrastructure.labels" . | nindent 4 }}
spec:
  serviceName: {{ .Values.postgres.auth.name }}
  replicas: {{ .Values.postgres.auth.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.postgres.auth.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.postgres.auth.name }}
        tier: infrastructure
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.postgres.auth.image.repository }}:{{ .Values.postgres.auth.image.tag }}"
          imagePullPolicy: {{ .Values.postgres.auth.image.pullPolicy }}
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: {{ .Values.postgres.auth.database }}
            - name: POSTGRES_USER
              value: {{ .Values.postgres.auth.username }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.postgres.auth.password }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            {{- toYaml .Values.postgres.auth.resources | nindent 12 }}
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.postgres.auth.username }}
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.postgres.auth.username }}
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
  {{- if .Values.postgres.auth.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - {{ .Values.postgres.auth.persistence.accessMode }}
        storageClassName: {{ .Values.global.storageClass }}
        resources:
          requests:
            storage: {{ .Values.postgres.auth.persistence.size }}
  {{- else }}
      volumes:
        - name: postgres-data
          emptyDir: {}
  {{- end }}
{{- end }}
{{- end }}
