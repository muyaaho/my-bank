{{- if .Values.services.authService.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.authService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.services.authService.name }}
    tier: backend
    {{- include "services.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.authService.port }}
      targetPort: {{ .Values.services.authService.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.services.authService.name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.authService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.services.authService.name }}
    tier: backend
    {{- include "services.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.authService.replicas | default .Values.common.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.authService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.authService.name }}
        tier: backend
        version: {{ .Values.services.authService.image.tag }}
      {{- if .Values.istio.enabled }}
      annotations:
        sidecar.istio.io/inject: "{{ .Values.istio.sidecarInjection }}"
      {{- end }}
    spec:
      containers:
        - name: {{ .Values.services.authService.name }}
          image: "{{ .Values.services.authService.image.repository }}:{{ .Values.services.authService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.authService.port }}
              name: http
          env:
            {{- range $key, $value := .Values.services.authService.env }}
            - name: {{ $key }}
              value: {{ tpl $value $ | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.common.resources | nindent 12 }}
          {{- if .Values.common.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.services.authService.port }}
            initialDelaySeconds: {{ .Values.common.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.common.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.common.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.common.livenessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.common.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.services.authService.port }}
            initialDelaySeconds: {{ .Values.common.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.common.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.common.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.common.readinessProbe.failureThreshold }}
          {{- end }}
{{- end }}
