spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test_mybank_pfm

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest

logging:
  level:
    com.mybank: DEBUG
