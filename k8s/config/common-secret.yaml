apiVersion: v1
kind: Secret
metadata:
  name: common-secret
  namespace: mybank
type: Opaque
stringData:
  # PostgreSQL credentials
  POSTGRES_PASSWORD: "mybank123"
  POSTGRES_USERNAME: "mybank"

  # MongoDB credentials (production-ready)
  MONGODB_USERNAME: "mybank"
  MONGODB_PASSWORD: "mybank123"
  MONGODB_AUTH_DATABASE: "admin"

  # Redis password (production-ready)
  REDIS_PASSWORD: ""

  # JWT Secret
  JWT_SECRET: "mybank360-super-secret-key-for-jwt-token-generation-minimum-256-bits-production-change-this"
  JWT_EXPIRATION: "86400000"
